#include "stdafx.h"
using namespace std;

int sum(int a, int b);
int sub(int a, int b);
int mul(int a, int b);

int main() {
	cout << "sum(2,3) = " << sum(2, 3) << endl;
	cout << "sub(2,3) = " << sub(2, 3) << endl;
	cout << "mul(2,3) = " << mul(2, 3) << endl;
	system("pause");
	return 0;
}

